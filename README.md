# sinatraExample

## How to run:

### Ensure MongoDB is running:

```mongod --dbpath [path/to/db] --nojournal```

### Use Bundler

```gem install bundler```

```bundler install```

### Start app:

```ruby exampleapp.rb```
